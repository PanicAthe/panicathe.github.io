# 변경 체크리스트

작업 마무리 전에 빠르게 확인할 항목입니다.

- 현재 컴포넌트 및 파일 구조와 맞는 수정인가
- 새 스타일이 가까운 `.css` 파일에 들어갔는가
- 생성 파일을 수동으로 수정하지 않았는가
- 승인 없이 파일을 삭제하지 않았는가
- `npm run lint`를 실행했거나, 못 했다면 이유를 남겼는가
- 빌드에 영향 있는 수정이면 `npm run build`까지 확인했는가
